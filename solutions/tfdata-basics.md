# Soluzione: tfdata-basics

La soluzione completa, spiegata riga per riga, sta dentro il notebook della
lezione (`notebooks/lezione-14-tf-data.ipynb`), subito dopo la cella "Prova tu". Le risposte
al quiz sono nel blocco richiudibile "Apri le risposte".
