---
lesson_id: capstone-gemma-lora
reviewed_at: 2026-07-18
claims:
  - id: capstone-gemma-lora-main
    claim: "The relations component uses a LoRA-adapted open model constrained to structured output, with a rule-based fallback (a known verb between two entities) so the component works even without the model."
    importance: critical
    source_url: "https://keras.io/keras_hub/api/models/gemma/"
    source_title: "Gemma models — KerasHub"
    publisher: "Keras documentation"
    accessed_at: "2026-07-18"
    primary_source: true
    evidence_note: "Integration lesson of the capstone; the cited source anchors the reused technique. NOT verified live this session (network not tested); needs_reverification per COURSE_FACTORY_SPEC.md 3.2. NOT executed against the real gated model this session; the guarded cell is skipped and the rule-based fallback runs. needs_reverification per COURSE_FACTORY_SPEC.md 3.2."
    status: needs_reverification
  - id: gemma-backbone-enable-lora-signature
    claim: "GemmaBackbone.enable_lora(rank, target_layer_names=None) freezes all backbone weights while enabling LoRA on the query and value EinsumDense layers of the attention blocks; if target_layer_names is None it defaults to the layer names returned by backbone.default_lora_layer_names(), and rank sets the rank of the LoRA factorization."
    importance: minor
    source_url: "https://keras.io/keras_hub/api/models/gemma/gemma_backbone/"
    source_title: "KerasHub documentation — GemmaBackbone.enable_lora"
    publisher: "Keras documentation"
    published_or_updated: null
    accessed_at: "2026-08-11"
    primary_source: true
    evidence_note: "Documentazione KerasHub letta in questa sessione (fonte ufficiale della libreria, non un blog): conferma la firma `enable_lora(rank, target_layer_names=None)`, che congela il backbone e abilita LoRA sui soli layer query/value. Il notebook chiama `gemma.backbone.enable_lora(rank=4)` nella cella guardata (Gemma non scaricabile in questo ambiente); la claim documenta cosa farebbe la chiamata, non un'esecuzione reale del modello."
    status: verified
review:
  reviewer: claude
  decision: pass
  notes:
    - "Cella Gemma guardata (needs_reverification, vedi research_gaps.md); il fallback a regole e' eseguito davvero. All numbers quoted in the docs page are genuine outputs from executing notebooks/lezione-56-capstone-gemma-lora.ipynb this session, not invented."
    - "2026-08-11 (WI-6, segmento 55-57): la teoria aggiunge il riscontro fra `enable_lora(rank=4)` nella cella guardata e la firma ufficiale KerasHub (quali layer vengono adattati, cosa fa il parametro rank), verificata live in questa sessione. Nessuna cella di codice toccata. Nota: la claim gia' esistente in questo pack (capstone-gemma-lora-main) resta needs_reverification e non e' stata auditata: fuori scope, WI-6 aggiunge fonti a nuove affermazioni."
