---
lesson_id: structured-output
reviewed_at: 2026-08-09
claims:
  - id: validate-not-trust
    claim: "Because a language model emits free text, structured-output pipelines must extract the JSON block, parse it, and validate it against a schema (repairing or rejecting non-conforming output) rather than trusting the raw generation."
    importance: critical
    source_url: "https://docs.python.org/3/library/json.html"
    source_title: "json — JSON encoder and decoder"
    publisher: "Python documentation"
    accessed_at: "2026-07-18"
    primary_source: true
    evidence_note: "json.loads raises json.JSONDecodeError on malformed input, which is exactly what makes a validate-then-accept pipeline necessary; documented in the standard library. The 'don't trust the model output' practice is a widely-held engineering principle, framed didactically here."
    status: needs_reverification
  - id: json-syntax-boundary
    claim: "JSON parsing establishes syntactic validity but does not establish application-level constraints such as required Memory AI fields or allowed enum values."
    importance: critical
    source_url: "https://www.rfc-editor.org/rfc/rfc8259"
    source_title: "RFC 8259 — The JavaScript Object Notation (JSON) Data Interchange Format"
    publisher: "IETF"
    accessed_at: "2026-08-09"
    primary_source: true
    evidence_note: "Lo standard definisce grammatica e valori JSON; i vincoli applicativi restano esterni al parsing."
    status: verified
  - id: json-schema-validation-keywords
    claim: "JSON Schema validation can express application constraints including required properties, value types and enumerated allowed values."
    importance: critical
    source_url: "https://json-schema.org/draft/2020-12/json-schema-validation.html"
    source_title: "JSON Schema Validation: A Vocabulary for Structural Validation of JSON"
    publisher: "JSON Schema"
    accessed_at: "2026-08-09"
    primary_source: true
    evidence_note: "La specifica ufficiale definisce, tra gli altri, i keyword type, enum e required."
    status: verified
  - id: json-decoder-errors
    claim: "Python json.loads deserializes a JSON document and raises JSONDecodeError when the document is invalid."
    importance: major
    source_url: "https://docs.python.org/3/library/json.html"
    source_title: "json — JSON encoder and decoder"
    publisher: "Python documentation"
    accessed_at: "2026-08-09"
    primary_source: true
    evidence_note: "Il contratto e l'eccezione del decoder sono documentati dalla standard library."
    status: verified
review:
  reviewer: claude
  decision: pass
  notes:
    - "The Gemma call in notebooks/lezione-36-output-strutturato.ipynb is guarded and NOT executed here; the fallback uses a realistic 'dirty' example output. The whole validator/repair logic (regex JSON isolation, json.loads, schema check against Lesson 22's fields) genuinely runs and asserts on valid/invalid cases. json.JSONDecodeError behavior is standard-library, marked needs_reverification only for the live-network principle per 3.2."
    - "2026-08-09: ampliata la teoria sui confini fra estrazione, parsing e validazione e sulla politica ripara/rifiuta; fonti normative registrate nello stesso commit."
